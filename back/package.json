{
    "type": "module",
    "scripts": {
        "dev": "bun run --hot src/index.ts",
        "light": "bun run src/scripts/lighthouse.ts",
        "migrate" :"bun run src/scripts/migrate.ts",
        "test": "bun run src/test.ts"
    },
    "dependencies": {
        "hono": "^3.7.2",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "bun-types": "^0.6.2"
    }
}
